# VOILE Backend — Python Dependencies
# Optimized for 8 GB RAM Ubuntu laptop, $0 cost

# HTTP layer
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# Background job queue
rq==1.16.2
redis==5.0.8

# Image validation (lightweight, no torch)
Pillow>=10.4.0

# VTON provider: Hugging Face Spaces via Gradio Client
gradio_client==1.3.0

# .env file loading
python-dotenv>=1.0.0

# Redis fallback for local dev
fakeredis>=2.21.0

# fal.ai stub (uncomment when ready)
# fal-client==0.4.1
